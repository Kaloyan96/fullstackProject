import React, { useState, ReactElement } from 'react';

interface Props {
    // onSearchPosts: StringCallback;
}

export default function ABSSection({ ...rest }: Props): ReactElement<Props> {
    return (
        <div>
            hello
        </div>
    );
}