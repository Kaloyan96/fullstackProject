import React, { useState, ReactElement } from 'react';

interface Props {
    // onSearchPosts: StringCallback;
}

export default function HomePageSection({ ...rest }: Props): ReactElement<Props> {
    return (
        <div>
            hello
        </div>
    );
}